#include <stdio.h>
#include <stdlib.h>
/**
* main - prints its name even when its when renamed
* @argc: argument count
* @argv: pointer to string of array
*
* Description: A programme that prints it name and end with
* a new line and prints the new name when renamed.
* Return: Always zero on success.
*/
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);

return (0);
}
